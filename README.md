# ⏱️ Timer App

A simple timer application built with React. It demonstrates interval handling, state management with Hooks, and proper effect cleanup to prevent memory leaks.

**Tech stack:** React, JavaScript (JSX), CSS Modules (SCSS), HTML5.
